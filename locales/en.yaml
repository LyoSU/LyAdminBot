private:
  start: |
    Hey <b>${name}</b>! 👋

    <b>LyAdminBot</b> — your group moderation buddy.

    🛡 <b>What I do:</b>
    • Catch spam & ads
    • Ban scammers
    • Clean suspicious links

    Add me to your group and make me admin — I'll handle the rest.

    /help — all commands
  btn_add: Add to group
cmd:
  help: |
    🤖 <b>LyAdminBot</b>

    <b>ANTI-SPAM:</b>
    <code>!spam</code> — settings
    <code>!spam on/off</code> — toggle
    <code>!spam globalban on/off</code> — global ban
    <code>!cas</code> — CAS protection
    <code>!banChannel</code> — ban channels

    <b>MODERATION:</b>
    /banan — ban  •  /kick — kick  •  /del — delete
    /report or @admin — check for spam (reply)

    <b>STATS:</b>
    /mystats — your stats
    /top — most active  •  /top_banan — most bans

    <b>WELCOME:</b>
    <code>!welcome</code> — toggle
    <code>!gif</code> / <code>!text</code> — add content

    <b>OTHER:</b>
    <code>!extra name</code> — quick commands #name
    /extras — list  •  /lang — language  •  /ping — status
  my_stats:
    send_pm: Stats sent to DM 📊
    chat: |
      📊 ${name}'s stats in <b>${chatName}</b>:

      🍌 <b>Bananas:</b> ${banCount}
      ⏱ <b>Total ban time:</b> ${banTime}
      ⚡ <b>Autoban:</b> ${banAutoTime}

      💬 <b>Messages:</b> ${messages}
      📈 <b>Activity:</b> ${active}%
      🌊 <b>Flood:</b> ${flood}%

      📅 <b>Here since:</b> ${createdAt}
    error:
      blocked: |
        Can't DM you 😅
        Message me first, then try again.
  top:
    info: |
      🏆 Most active in <b>${chatName}</b>:
      ${top}
  top_banan:
    info: |
      🍌 Top bans in <b>${chatName}</b>:
      ${top}
    error:
      empty: No one got 🍌 here yet
  extras:
    list: |
      📝 <b>Extra commands:</b>
      ${extras}
    error:
      not_found: |
        No extra commands yet.
        Admin can create: <code>!extra name</code>
  ping:
      pong: |
        🏓 Pong!
        <b>Server:</b> ${sms} ms
        <b>Telegram:</b> ${tms} ms
        <b>Uptime:</b> ${workTime}

        ${extra}
      delay: |
        ❗️ <b>Bot was offline:</b> ${delayTime}
  welcome:
    disable: Welcome messages off 👋
    enable: Welcome messages on 👋
  gif:
    push: Gif added ✓
    pull: Gif removed ✓
    reset: All gifs cleared ✓
  text:
    push: Text added ✓
    pull: Text removed ✓
    error: Text must contain <code>%name%</code>
    reset: All texts cleared ✓
  extra:
    push: \#${extraName} saved ✓
    pull: \#${extraName} deleted ✓
    max: Max extras per message — <b>${maxExtra}</b>
    error:
      not_found: |
        \#${extraName} not found.
        To create — reply to a message with the command.
    set:
  members:
    member: |
      <a href="tg://user?id=${telegram_id}">${telegram_id}</a>
      ${banTime} | a: ${active} | f: ${flood}
  quote: |
    For quotes use @QuotLyBot
  cas:
    disable: CAS off
    enable: CAS on
  banChannel:
    disable: Channel ban off
    enable: Channel ban on
  spam_settings:
    enabled: "✅ <b>Anti-spam on</b>"
    disabled: "❌ <b>Anti-spam off</b>"
    allow:
      added: "✅ Allowed: \"${rule}\""
      error: "❌ Specify rule text\n\n<b>Example:</b> <code>!spam allow coursework sales</code>"
    deny:
      added: "✅ Blocked: \"${rule}\""
      error: "❌ Specify rule text\n\n<b>Example:</b> <code>!spam deny earning schemes</code>"
    remove:
      success: "✅ Rule removed: \"${rule}\""
      error: "❌ Invalid rule number\n\nCheck list: <code>!spam</code>"
    clear: "✅ Rules cleared (was: ${count})"
    globalban:
      enabled: "✅ <b>Global ban on</b>\nSpammers from other groups will be banned here too"
      disabled: "❌ <b>Global ban off</b>\nBans only apply to this group"
      error: "Use <code>!spam globalban on</code> or <code>!spam globalban off</code>"
    status:
      title: "🛡 <b>Anti-spam settings</b>"
      enabled_text: "✅ On"
      disabled_text: "❌ Off"
      globalban_enabled: "✅ Yes"
      globalban_disabled: "❌ No"
      rules_title: "Rules (${count}):"
      rules_empty: "Not set"
      rule_allow: "✅ Allow"
      rule_deny: "❌ Block"
      commands_title: "Commands:"
      trusted_users: "users"
      commands: |
        <code>!spam on</code> — enable
        <code>!spam off</code> — disable
        <code>!spam globalban on/off</code> — global ban
        <code>!spam trust</code> — trust user (reply)
        <code>!spam untrust</code> — untrust user (reply)
        <code>!spam allow text</code> — allow
        <code>!spam deny text</code> — block
        <code>!spam remove N</code> — remove rule
        <code>!spam clear</code> — clear all
    trust:
      usage: "<b>How to use:</b>\n• Reply: <code>!spam trust</code>\n• Username: <code>!spam trust @username</code>\n• ID: <code>!spam trust 123456</code>"
      cant_trust_bot: "🤖 Can't trust bots"
      already_trusted: "✅ ${name} already trusted"
      added: "✅ ${name} now trusted"
      user_not_found: "❌ Can't find ${username}"
    untrust:
      usage: "<b>How to use:</b>\n• Reply: <code>!spam untrust</code>\n• Username: <code>!spam untrust @username</code>\n• ID: <code>!spam untrust 123456</code>"
      not_trusted: "❌ ${name} wasn't trusted anyway"
      removed: "✅ ${name} no longer trusted"
      user_not_found: "❌ Can't find ${username}"
  reset: Settings reset ✓
settings:
    json:
      reset: Settings restored from file ✓
banan:
  show: ${name} shows 🍌
  suc:  ${name} gets 🍌\n<b>For:</b> ${duration}
  pick: ${name} lost 🍌
  error: <b>Couldn't give 🍌</b>\n<pre>${error}</pre>
kick:
  suc: ${name} gets kicked 👢
  who: Who to kick? 🤔
  error: <b>Couldn't kick</b>\n<pre>${error}</pre>
del:
  error: <b>Couldn't delete</b>\n<pre>${error}</pre>
sticker:
  save:
    suc: Sticker added to <a href="${link}">group pack</a> ✓
    error:
      animated: Animated stickers not supported 😅
      telegram: <b>Couldn't save sticker</b>\n<pre>${error}</pre>
  delete:
    suc: Sticker removed from <a href="${link}">pack</a> ✓
    error:
      telegram: <b>Couldn't delete sticker</b>\n<pre>${error}</pre>
  empty_forward: |
    Reply to a sticker, photo, or file with the command.
  only_admin: |
    Admins only.
    For personal pack — @fStikBot
cas:
  banned: |
    ${name} banned — CAS listed.
    <a href="${link}">Details</a>

spam:
  banned: "🚫 ${name} permabanned\n<code>${reason}</code>"
  muted: "🔇 ${name} muted for 24h\n<code>${reason}</code>"
  notification:
    full: "🚫 ${name} — spam\n<code>${reason}</code>"
    muted_only: "🔇 ${name} muted\n<code>${reason}</code>\n\n⚠️ Couldn't delete message"
    deleted_only: "🗑 Spam deleted\n<code>${reason}</code>\n\n⚠️ ${name} not muted"
    no_permissions: "👀 Looks like spam from ${name}\n<code>${reason}</code>\n\n💡 Make me admin and I'll handle it"
  error:
    mute_failed: |
      ❌ <b>Couldn't mute ${name}</b>
      Reason: ${reason}
    delete_failed: |
      ❌ <b>Couldn't delete spam from ${name}</b>
      Reason: ${reason}
    partial_success_muted: |
      ✅ ${name} muted for spam
      Reason: ${reason}
      ⚠️ Couldn't delete message
    partial_success_deleted: |
      ✅ Spam deleted
      ⚠️ Couldn't mute ${name}
      Reason: ${reason}

only_group: Groups only
only_admin: Admins only

global_ban:
  kicked: "🚫 ${name} kicked — global ban.\n<code>${reason}</code>"

report:
  only_group: "Groups only"
  need_reply: "↩️ Reply to a message"
  cant_report_bot: "🤖 That's a bot"
  cant_report_self: "🙃 Can't report yourself"
  cant_report_admin: "👮 That's an admin"
  invalid_target: "Can't identify sender"
  rate_limited: "⏳ Wait ${seconds}s"
  spam_check_disabled: "Anti-spam is off in this group"
  no_content: "No text to check"
  analyzing: "🔍 Checking..."
  spam_found: "🚫 <b>Spam</b> from ${target}\n<code>${reason}</code>\n\n${action}\n\n<i>📣 ${reporter}</i>"
  suspicious: "🤔 <b>Suspicious</b> — ${target}\n<code>${reason}</code>\n\n<i>📣 ${reporter}</i>"
  clean: "✅ ${target} — ok"
  error: "Something went wrong"
